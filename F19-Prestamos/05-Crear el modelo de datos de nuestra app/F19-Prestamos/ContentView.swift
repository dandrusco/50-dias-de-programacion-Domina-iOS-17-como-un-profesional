//
//  ContentView.swift
//  F19-Prestamos
//
//  Created by Danilo Andrusco on 21-11-24.
//

//1. Ahora es el turno de meter el JSON en nuestra app, para ello crearemos un nueva carpeta llamada Model, dentro crearemos un archivo de Swift File que lo llamaremos Loan
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
